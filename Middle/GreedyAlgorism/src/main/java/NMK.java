import java.util.Scanner;

public class NMK {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int inputNumber = scanner.nextInt();
        int increaseLength = scanner.nextInt();
        int decreaseLength = scanner.nextInt();

        if (inputNumber < increaseLength + decreaseLength) {
            System.out.println(-1);
        } else {
            if (increaseLength == 1) {
                
            }
        }
    }
}
